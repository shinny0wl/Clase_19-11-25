on: push
jobs:
    build-node:
      runs-on: ubuntu-latest
      container: cassandra
      steps:
        - run: echo "Tarea 8"
        - run: cqlsh -e "show VERSION"
        - run: echo "Proceso terminado"